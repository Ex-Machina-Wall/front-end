version: '3.8'
services:
  react-app:
    build: .
    ports:
      - "3006:3006"
    volumes:
      - /app/node_modules
      - .:/app
    environment:
      - NODE_ENV=production
    restart: unless-stopped